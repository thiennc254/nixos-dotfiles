-- =========================================================================
-- MONITORS CONFIGURATIONS
-- =========================================================================
for _, config in pairs(hypr.shared.monitors) do
	hl.monitor(config)
end
